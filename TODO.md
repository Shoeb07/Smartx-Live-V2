# TODO

- [x] Inspect blog routing (`/blog/[slug]`) to confirm sitemap URL prefix (implemented: redirect to `/${slug}`, so sitemap uses root slug paths)
- [x] Update `src/app/sitemap.ts` to remove invalid service URLs and generate blog URLs from `src/lib/blog-posts.ts`
- [x] Run `next lint`/`next build`/`tsc` to validate



